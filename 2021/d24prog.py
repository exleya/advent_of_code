def prog(input):
    w,x,y,z = 0,0,0,0
    w = int(input[0])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 1
    x = x + 15
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 15
    y = y * x
    z = z + y
    w = int(input[1])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 1
    x = x + 15
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 10
    y = y * x
    z = z + y
    w = int(input[2])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 1
    x = x + 12
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 2
    y = y * x
    z = z + y
    w = int(input[3])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 1
    x = x + 13
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 16
    y = y * x
    z = z + y
    w = int(input[4])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -12
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 12
    y = y * x
    z = z + y
    w = int(input[5])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 1
    x = x + 10
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 11
    y = y * x
    z = z + y
    w = int(input[6])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -9
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 5
    y = y * x
    z = z + y
    w = int(input[7])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 1
    x = x + 14
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 16
    y = y * x
    z = z + y
    w = int(input[8])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 1
    x = x + 13
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 6
    y = y * x
    z = z + y
    w = int(input[9])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -14
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 15
    y = y * x
    z = z + y
    w = int(input[10])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -11
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    w = int(input[11])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -2
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 12
    y = y * x
    z = z + y
    w = int(input[12])
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -16
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 10
    y = y * x
    z = z + y
    w = int(input[13])
    x = x * 0   
    x = x + z
    x = x % 26  # x = z % 26
    z = z // 26 # z = z // 26
    x = x + -14 # x = x - 14
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1  # y = 25 * x + 1  
    z = z * y  # z = z * (25 * x + 1)
    y = y * 0
    y = y + w
    y = y + 13 # need z == -((w+13) * x)
    y = y * x  # need z == -(y*x)
    z = z + y  # need z == 0 need z == -y at this point.
    return w,x,y,z
